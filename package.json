{
  "name": "start-it",
  "version": "1.0.0",
  "description": "Link launcher desktop app built with Electron, React, Tailwind, and SQLite",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently \"npm --prefix renderer run dev\" \"wait-on tcp:5173 && electron .\"",
    "build": "npm --prefix renderer run build",
    "start": "electron .",
    "lint": "npm --prefix renderer run lint"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^33.2.0",
    "wait-on": "^8.0.1"
  }
}
